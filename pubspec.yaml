name: flutter_purple_impl
description: "A new Flutter project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.9.0
  flutter: ">=1.17.0"

dependencies:
  flutter_purple_domains:
    git: git@github.com:boginni/flutter_purple_domains.git

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:
